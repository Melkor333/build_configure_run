#!/usr/bin/env ysh

# TODO: Proper argparse stuff
# TODO TODO: argparse snippet :D
var file = ARGV[0]

#var connections = $(cat $file)=>split('\n')
var items = $(grep -v '#' $file | sed 's/ /\n/' | sort -u)=>split(u'\n')
cat ./templates/head
for item in @items {
  grep "  $item" templates/classes
}
sed 's/ /-->/' $file | grep -v '^#'


