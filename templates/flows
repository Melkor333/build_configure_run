code -.-> generate
generate -.-> blobs
inputs -.-> generate
inputs -.-> store
blobs -.-> store
store -.-> storage
blobs -.-> deploy
storage -.-> deploy
deploy -.-> deployment
deployment -.-> configure
configure -.-> service
service -.-> emit
emit -.-> event
event -.-> trigger
trigger -.-> deployment
service -.-> monitor
monitor -.-> metrics
metrics -.-> deployment
deploy -.-> snapshot
snapshot -.-> deploy
deployment -.-> manage
manage -.-> service
event -.-> change
change -.-> inputs
change -.-> deployment
change -.-> storage
change -.-> code
code -.-> deploy
code -.-> configure
inputs -.-> deploy
service -.-> control
control -.-> process
code -.-> control
inputs -.-> control
