node code
node inputs
node storage
node deployment
node service
node event
node snapshot
node "blobs and ir" as blobs
node "metrics and logs" as metrics
node process

interface generate
interface manage
interface store
interface deploy
interface control
interface emit
interface trigger
interface change
interface monitor
